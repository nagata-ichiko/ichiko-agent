# ゴール: 実装中に質問が発生する残存3経路（問い詰め漏れ・既存コード矛盾・自己申告ゲート）を塞ぐ

<!--
ゴールボード（生きたドキュメント）。plan フェーズで作成し、build が進捗・ブロッカー・現在地を随時更新する。
配置: impl-plans/SPEC-HARNESS-001.md（作業中の一時ファイル。完了後は削除してよい）
-->

## 受入基準（ゴール到達の判定）

- 要件カード SPEC-HARNESS-001 §3 の全 Given-When-Then を満たす（M6-1: 5件 / M6-2: 3件 / M6-3: 6件）

## マイルストーン

- [x] M1: spec-lint スクリプト作成 — 完了判定: §6 の入力契約どおりに動作し、不備カード4種（§0未達/表空/(a)未決/曖昧語）で非0・完成カード（SPEC-HARNESS-001自身）で0を返すテストが通る — 状態: **完了**（commit `58973f6`、テスト9ケース全PASS）
  - [x] `.claude/scripts/spec-lint.sh` 作成（bash+python3、hooks と同じ構成）
  - [x] テスト: 正常2 + 不備4 + (b)のみ合格1 + 入力エラー2 の9ケースを `.claude/scripts/tests/spec-lint.test.sh` で機械検証
- [x] M2 [P]: grill タイプ表の拡充 — 完了判定: grill SKILL.md のタイプ判定表に「外部サービス連携/環境変数・secrets/エラー・リトライ・キャッシュ方針/性能数値/デザイン方針(UI時)」の行が追加され、記録先セクション（§3/§6/§10）が明記されている — 状態: **完了**（commit `1a69b26`）
  - [x] タイプ判定表に5行追加（判定基準・必須項目・追加で聞くこと）
  - [x] CLAUDE.md テンプレート（ルート）に「## デザイン方針」記入欄を追加（レビューF5解消）
- [x] M3 [P]: 既存コード読み込みの必須化 — 完了判定: grill SKILL.md に「変更系は該当既存コードを読み file:line で現状をカードに記録・矛盾は確定前に指摘」の Step が追加され、新規機能のスキップ条件も明記されている — 状態: **完了**（commit `be2c040`、新 Step 3・以降採番追従）
- [x] M4: 手順への lint 組み込みと整合 — 完了判定: grill の Fixゲート判定 Step と build 準備フェーズに spec-lint 実行が明記され、spec-card テンプレの記入ルールと矛盾がない（横断 grep で確認） — 状態: **完了**（横断 grep で参照整合・lint 再PASS 確認済み）
  - [x] grill Step 6 に「spec-lint 実行→非0なら潰して再実行」を追記
  - [x] build 準備フェーズ 1. に「spec-lint で Fix ゲートを機械検証」を追記
  - [x] principles §1 Fixゲートの記述に機械検証を反映

## 流用する既存コード

- `.claude/hooks/validate-command.sh:11-12` — stdin/引数処理と python3 併用の構成パターン（spec-lint も bash+python3 で揃える）
- `docs/templates/spec-card.md:15-25` — §0 チェックリストの正確な文言（lint の判定対象）
- `.claude/skills/grill/SKILL.md:33-43` — 既存タイプ判定表（行追加のベース）

## やらないこと（WHY NOT）

- 受動起動（Actions/@claude/Routines）— ユーザー判断で不要（自律=後から不明点が出ないこと）
- spec-lint の CI 自動実行・フック強制 — まず手動運用で摩擦を観測してから（M7候補）
- spec-card テンプレのセクション番号変更 — §7/§8 参照が skills/rules に多数（既存参照破壊を回避）
- implementation.md 既定判断表の削除 — 緊急modeC の例外時指針として存続

## テスト計画

- M1: lint 自己テスト7ケース（スクリプト化して logs/ へ出力、チャットは要約）
- M2〜M4: 文書変更のため、受入基準との照合 + 横断 grep（旧文言残存・参照整合）で検証
- 仕上げ: SPEC-HARNESS-001 自身に spec-lint を実行して 0 を確認（ドッグフーディング）

## 現在地 / ブロッカー / 次アクション

- 現在地: 全マイルストーン完了・受入基準14件を実装と照合済み・PR待ち
- ブロッカー: なし
- 次アクション: PR 作成（ユーザー承認後）→ マージ後に本ファイル削除
