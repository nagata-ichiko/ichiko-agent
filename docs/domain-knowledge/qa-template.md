# Q&A テンプレート

このファイルをコピーして、テーマ別ファイル（例: `billing.md`）を作るか、既存ファイルに追記する。
1エントリ = 1つの「調べ直しが起きがちな知識」。

---

## 用語: 〇〇とは

**結論:** （1〜2行で。何を指すか・どう扱うか）

**根拠:**
- `path/to/file.ts:42` — （このコードで何が確認できるか）
- `docs/design/db-design.md:15` — （設計書側の定義）

**補足:** （混同しやすい類似概念・注意点。例:「△△とは別物。△△は〜」）

---

## テーブル: xxx_table

**役割:** （このテーブルが何を保持するか）

**主要カラム:**
| カラム | 意味 | 補足 |
|--------|------|------|
| `status` | 状態 | `0=未処理, 1=処理中, 2=完了`（`path/to/enum.ts:10`） |

**関連:**
- `yyy_table` と `xxx_id` で 1:N（FK: `path/to/migration.rb:8`）

**根拠:** `db/schema.rb:120`

**ハマりポイント:** （例: 論理削除は `deleted_at` で表現。物理削除しない）

---

## 仕様: 〇〇の挙動

**質問:** （実際に調べることになった問い。例:「通知はメール？アプリ内通知？」）

**結論:** （確定した答え）

**根拠:**
- `app/services/notifier.rb:30` — （該当ロジック）

**判断時の注意:** （スコープを誤りやすい点。例:「"通知"はアプリ内通知を指す。メールは別系統 `mailer.rb`」）
